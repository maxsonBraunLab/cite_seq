---
__default__:
  time: '12:00:00'
  nodes: 1
  partition: exacloud
  job-name: "{rule}"
  output: "logs/{rule}/{wildcards}.out"
  error: "logs/{rule}/{wildcards}.err"
  cpus-per-task: 8
  mem: 8G